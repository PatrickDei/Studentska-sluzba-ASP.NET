@model StudentskaSluzba.Model.Professor

@{
    ViewData["Title"] = "Uređivanje profesora";
}

<div class="row">
    <div class="col-6">
        <form asp-action="EditClasses">
            <input type="hidden" asp-for="ID" />
            <partial name="_CreateOrEdit" />
        </form>
    </div>
</div>